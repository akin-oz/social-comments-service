/**
 * Application entry point placeholder.
 * Future responsibility: compose configuration, dependencies, Fastify, routes, and graceful shutdown.
 */
export function createApplication(): void {
  // TODO: compose the application after the domain and adapter contracts are implemented.
}
