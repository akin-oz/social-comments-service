/**
 * Placeholder for Fastify route registration.
 * Future responsibility: validate HTTP input, invoke application use cases, and map errors to API responses.
 */
export interface ApiRouteRegistrar {
  register(): void;
}
