# Generated artifacts belong to their generators, not to the formatter.
# Reformatting them only creates churn the next time they are regenerated.
pnpm-lock.yaml
docs/openapi.json
dist/
# The ai-engineering compiler's ownership state. Formatting it by hand sets up
# ping-pong between this gate and the CI drift gate the next time sync rewrites
# it in the compiler's own style.
.ai/state/
