---
description: Run the repository verification gate.
---

Run `pnpm typecheck`, `pnpm lint`, `pnpm format:check`, `pnpm test`, and `pnpm ai:validate`. Report every command and its result. Do not bypass failures or modify source to make the gate pass.
